# Python
# Setup
Clone
git clone -----------
git checkout -b NOM_BRANCHE
git merge master

Code
git checkout master
git pull
git checkout NOM_BRANCHE
git merge master

Comme comme tu veux -> Commit

git push
